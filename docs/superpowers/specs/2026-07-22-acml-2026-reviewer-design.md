# ACML 2026 Reviewer Service Entry

## Scope

Add ACML 2026 to the existing `Conference Reviewer` list on the homepage.

## Design

The Services section in `index.html` will gain one line directly after the
existing conference-reviewer entries:

`Asian Conference on Machine Learning (ACML 2026)`

The entry intentionally does not mention the individual tracks or add a news
item, so it matches the concise format of the other conference-service entries.

## Verification

Inspect the resulting HTML to confirm the new entry is within the `Conference
Reviewer` block and that no other homepage content changed.
